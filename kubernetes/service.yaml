apiVersion: v1
kind: Service
metadata:
  name: Petclinic
spec:
  selector:
    app: Petclinic
  type: NodePort
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30000